# Copy this file to `.env` and fill in your key.
# The agent automatically loads `.env` on startup — no `source .env` needed.

export OPENAI_API_KEY="your-openai-api-key"
export CLI_AGENT_ALLOWED_COMMANDS="ralio"
